# Inline SVG symbol source

This folder stores inline SVG factories used by the skinnable slot UI.

The runtime theme engine maps each `SymbolId` to an SVG string so Storybook demos remain self-contained and do not depend on external image assets.
